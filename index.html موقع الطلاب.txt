<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>إنجازات الطلاب</title>
  <meta name="description" content="موقع يعرض إنجازات الطلاب والمشاريع والمسابقات المتميزة">
  <meta name="keywords" content="إنجازات, طلاب, مشاريع, مسابقات, جوائز">
  <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@600&display=swap" rel="stylesheet">
  <script src="https://kit.fontawesome.com/a2d9d6b6c9.js" crossorigin="anonymous"></script>
  <script src="https://cdn.jsdelivr.net/npm/three@0.152.0/build/three.min.js"></script>

  <style>
    body, html {
      margin: 0;
      padding: 0;
      overflow: hidden;
      font-family: 'Cairo', sans-serif;
      height: 100vh;
      color: white;
      background: #000010;
    }

    canvas {
      position: fixed;
      top: 0;
      left: 0;
      z-index: 0;
    }

    /* الرسالة الترحيبية */
    .welcome-message {
      position: fixed;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      background: rgba(0, 0, 0, 0.7);
      color: #00eaff;
      padding: 30px 60px;
      border-radius: 20px;
      font-size: 1.8em;
      text-align: center;
      z-index: 5;
      box-shadow: 0 0 30px #00eaff;
      animation: fadeInOut 10s ease forwards;
    }

    @keyframes fadeInOut {
      0% { opacity: 0; transform: translate(-50%, -50%) scale(0.8); }
      10% { opacity: 1; transform: translate(-50%, -50%) scale(1); }
      90% { opacity: 1; transform: translate(-50%, -50%) scale(1); }
      100% { opacity: 0; transform: translate(-50%, -50%) scale(0.8); }
    }

    .content {
      position: relative;
      z-index: 2;
      text-align: center;
      top: 18%;
      padding: 0 20px;
    }

    h1 {
      font-size: 3em;
      background: linear-gradient(90deg, #00c3ff, #0044ff);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      margin-bottom: 10px;
      animation: fadeInUp 1s ease forwards;
      opacity: 0;
    }

    /* العبارات المتحركة */
    .motto {
      font-size: 1.5em;
      color: #00eaff;
      text-shadow: 0 0 20px #00eaff;
      height: 40px;
      overflow: hidden;
      margin-bottom: 40px;
      animation: fadeInUp 1.3s ease forwards;
      opacity: 0;
    }

    .motto span {
      display: block;
      animation: slideText 20s infinite;
    }

    @keyframes slideText {
      0%, 10% { transform: translateY(0); }
      20%, 30% { transform: translateY(-100%); }
      40%, 50% { transform: translateY(-200%); }
      60%, 70% { transform: translateY(-300%); }
      80%, 100% { transform: translateY(0); }
    }

    .motto span:nth-child(1) { color: #00eaff; }
    .motto span:nth-child(2) { color: #4dff91; }
    .motto span:nth-child(3) { color: #ffcc00; }
    .motto span:nth-child(4) { color: #ff66cc; }

    p.intro {
      font-size: 1.3em;
      color: #b9dfff;
      line-height: 1.6em;
      margin-bottom: 40px;
      animation: fadeInUp 1.2s ease forwards;
      opacity: 0;
    }

    .menu {
      display: flex;
      justify-content: center;
      gap: 30px;
      margin-top: 20px;
      flex-wrap: wrap;
    }

    .menu-item {
      background: linear-gradient(145deg, rgba(255,255,255,0.15), rgba(255,255,255,0.05));
      border-radius: 20px;
      padding: 30px 40px;
      width: 220px;
      text-align: center;
      transition: 0.4s;
      box-shadow: 0 10px 30px rgba(0,0,0,0.4);
      backdrop-filter: blur(10px);
      cursor: pointer;
      text-decoration: none;
      color: white;
      animation: fadeInUp 1.5s ease forwards;
      opacity: 0;
    }

    .menu-item:hover {
      transform: translateY(-10px) scale(1.05);
      background: rgba(255,255,255,0.25);
    }

    .menu-item i {
      font-size: 3em;
      color: #00eaff;
      margin-bottom: 15px;
      transition: 0.3s;
    }

    .menu-item:hover i {
      color: #fff;
      text-shadow: 0 0 20px #00eaff;
    }

    .menu-item h3 {
      margin-bottom: 10px;
    }

    .menu-item p {
      font-size: 0.9em;
      color: #b9dfff;
    }

    footer {
      position: absolute;
      bottom: 20px;
      width: 100%;
      text-align: center;
      color: #b9dfff;
      z-index: 2;
      font-size: 0.9em;
    }

    @keyframes fadeInUp {
      from { transform: translateY(30px); opacity: 0; }
      to { transform: translateY(0); opacity: 1; }
    }
  </style>
</head>
<body>

  <canvas id="bg"></canvas>

  <!-- ✨ الرسالة الترحيبية ✨ -->
  <div class="welcome-message">
    مرحباً بكم في موقع طلاب مدرسة أبو هابيل 🎓
  </div>

  <div class="content">
    <h1>🌟 مدرسة ابو هابيل الثانوية للبنين</h1>
<h1>🌟قسم تكنولوجيا المعلومات BTEC</h1>

    <!-- ✨ العبارات المتحركة ✨ -->
    <div class="motto">
      <span>كن معنا في طريق التقدم والتميّز 🚀</span>
      <span>نحو مستقبلٍ تكنولوجي مشرق 💡</span>
      <span>الإبداع يبدأ من الفكرة وينتهي بالإنجاز 🎯</span>
      <span>معاً نحو الريادة والابتكار 🌍</span>
    </div>

    <p class="intro">طلاب الثاني ثانوي تكنولوجيا المعلومات</p>
    <p class="intro">اكتشفوا إبداعات طلابنا في المشاريع، المسابقات، والإنجازات المبتكرة. انضموا إلينا في رحلة العلم والإبداع!</p>

    <div class="menu">
      <a href="ما هو تخصص IT.HTML" class="menu-item">
        <i class="fas fa-video"></i>
        <h3> ما هو تخصص تكنولوجيا معلومات IT ؟</h3>
        <p>|ادخل لمرفة ما هو تخصص IT في نظام BTEC </p>
      </a>

      <a href="صور الابداعات.html" class="menu-item">
        <i class="fas fa-robot"></i>
        <h3>المشاريع التي شاركت بها المدرسة بالمسابقات</h3>
        <p>شاهد مشاريع الطلاب من مختلف الأنشطة</p>
      </a>

      <a href="رحلة الطلاب والمشاريع.html" class="menu-item">
        <i class="fas fa-laptop-code"></i>
        <h3>إنجازات الطلاب في مسار BTEC IT</h3>
        <p>مشاريع احترافية تجمع بين التقنية والإبداع</p>
      </a>
    </div>
  </div>

  <footer>© 2025 | موقع إنجازات الطلاب — جميع الحقوق محفوظة من قبل طلاب تكنولوجيا المعلومات ثاني ثانوي  IT - ❤️</footer>

  <script>
    // نجوم الخلفية
    const scene = new THREE.Scene();
    scene.fog = new THREE.Fog(0x000010, 200, 1000);
    const camera = new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight, 0.1, 1000);
    const renderer = new THREE.WebGLRenderer({ canvas: document.getElementById('bg'), alpha: true });
    renderer.setSize(window.innerWidth, window.innerHeight);

    const starGeometry = new THREE.BufferGeometry();
    const starCount = 800;
    const starPositions = new Float32Array(starCount * 3);
    for (let i = 0; i < starCount * 3; i++) starPositions[i] = (Math.random() - 0.5) * 2000;

    starGeometry.setAttribute('position', new THREE.BufferAttribute(starPositions, 3));
    const starMaterial = new THREE.PointsMaterial({ color: 0x00eaff, size: 2 });
    const stars = new THREE.Points(starGeometry, starMaterial);
    scene.add(stars);
    camera.position.z = 500;

    document.addEventListener('mousemove', (e) => {
      camera.rotation.x = (e.clientY / window.innerHeight - 0.5) * 0.5;
      camera.rotation.y = (e.clientX / window.innerWidth - 0.5) * 0.5;
    });

    function animate() {
      requestAnimationFrame(animate);
      stars.rotation.x += 0.0005;
      stars.rotation.y += 0.001;
      renderer.render(scene, camera);
    }
    animate();

    window.addEventListener('resize', () => {
      camera.aspect = window.innerWidth / window.innerHeight;
      camera.updateProjectionMatrix();
      renderer.setSize(window.innerWidth, window.innerHeight);
    });

    // إزالة الرسالة بعد 10 ثوانٍ
    setTimeout(() => {
      document.querySelector('.welcome-message').remove();
    }, 5000);
  </script>
</body>
</html>